# Optional fallback token for stdio mode or HTTP requests without Authorization header
ED_API_TOKEN=your_api_token_here

# Optional: set region (us, au, etc.) — defaults to "us"
ED_REGION=us
ED_READ_ONLY=false

# Optional for Docker/HTTP mode
MCP_TRANSPORT=http
HOST=0.0.0.0
PORT=8080
